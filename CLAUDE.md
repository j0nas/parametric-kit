<!--VITE PLUS START-->

# Using Vite+, the Unified Toolchain for the Web

This project is using Vite+, a unified toolchain built on top of Vite, Rolldown, Vitest, tsdown, Oxlint, Oxfmt, and Vite Task. Vite+ wraps runtime management, package management, and frontend tooling in a single global CLI called `vp`. Vite+ is distinct from Vite, and it invokes Vite through `vp dev` and `vp build`. Run `vp help` to print a list of commands and `vp <command> --help` for information about a specific command.

Docs are local at `node_modules/vite-plus/docs` or online at https://viteplus.dev/guide/.

## Review Checklist

- [ ] Run `vp install` after pulling remote changes and before getting started.
- [ ] Run `vp check` and `vp test` to format, lint, type check and test changes.
- [ ] Check if there are `vite.config.ts` tasks or `package.json` scripts necessary for validation, run via `vp run <script>`.
- [ ] If setup, runtime, or package-manager behavior looks wrong, run `vp env doctor` and include its output when asking for help.

<!--VITE PLUS END-->

# parametric-kit conventions

- Library extracted from parametric-mtg-deck-box and a4-stackable-trays (sibling checkouts under
  `~/Desktop/projects/3d/`) — when in doubt about intended behavior, read how those apps use the
  original copies.
- Run tests via `pnpm test` (project-local vp). A globally installed `vp` brings a second vitest
  and crashes with "Cannot read properties of undefined (reading 'config')".
- `src/csg` drives private manifold-3d bindings (`_ToPolygons`/`_Extrude`) as a WASM-leak
  workaround; it is validated against the pinned manifold-3d 3.5.1. Any manifold bump must run
  the csg tests before apps pick it up.
- Relative imports carry the `.ts` extension (allowImportingTsExtensions + nodenext).
- Subpath exports in package.json are GENERATED by `vp pack` (`pack.exports: true`); add new
  modules to `pack.entry` in vite.config.ts, never hand-edit the exports map.
- Comments only for constraints the code can't express; match the terse tone of the extracted
  sources.
- The Claude skill guiding consumers lives at `.claude/skills/parametric-kit/SKILL.md`; keep it
  in sync when public APIs change.
